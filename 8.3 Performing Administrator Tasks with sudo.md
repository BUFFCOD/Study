

sudo ls /root > ~/output.txt

This runs `ls` as root, but the redirection happens as your user. To run the whole pipeline as root, wrap it:

```bash
sudo sh -c "ls /root > ~/output.txt"
# or
sudo ls /root | sudo tee ~/output.txt > /dev/null
```
