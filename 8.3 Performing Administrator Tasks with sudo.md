

sudo ls /root > ~/output.txt


this command runs the first half of the command in sudo and then the rest as the local user you are login as

if you want to run the full command as sudo

sudo sh -c "ls/root > ~/output.txt"